describe('<RegisterServiceWorker />', () => {
  it('should register service worker', () => {

  });

  context('when there is a new version', () => {
    it('should call children as function with newVersion true', () => {

    });
  });

  context('when the versions are the same', () => {
    it('should call children as function with newVersion false', () => {

    });
  });

  context('when an error happens', () => {
    it('should call children as function with error object', () => {

    });
  });
});
